You are refactoring auto-generated React TSX from Figma into production-quality code.

Goals:
- Replace absolute positioning with flexbox/grid
- Extract reusable components (Button, Card, SidebarItem)
- Normalize spacing using a consistent scale (8px system)
- Convert hardcoded styles into Tailwind classes
- Preserve visual layout exactly

Constraints:
- No inline styles unless necessary
- No absolute positioning unless required
- Use semantic structure
- Make components reusable

Output:
- Clean component hierarchy
- One file per component


Figma has created a new design for the workspace page of the web app. The design files (*.tsx) is located in figma/src.
