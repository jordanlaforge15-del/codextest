{
  "name": "@apps/extension",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/background.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint 'src/**/*.ts'",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@mvp/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.280",
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2"
  }
}
